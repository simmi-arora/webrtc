/************************************************************************
Track Call Record 
*************************************************************************/
/**
 * collect all webrtcStats and stream to Server to be stored in a file with seesion id as the file name 
 * @method
 * @name sendCallTraces
 * @param {string} traces
 */
function sendCallTraces(){

}

/**
 * collect all console.log and stream to Server to be stored in a file with seesion id as the file name 
 * @method
 * @name sendConsoleLogs
 * @param {string} logs
 */
function sendConsoleLogs(){

}


if (document.getElementById("supportBtn")) {
    document.getElementById("supportBtn").onclick = function () {
        alert(" Support Requested ");
    };
}